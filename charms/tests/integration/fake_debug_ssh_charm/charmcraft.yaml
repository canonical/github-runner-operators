# Copyright 2026 Canonical Ltd.
# See LICENSE file for licensing details.

name: fake-debug-ssh

type: charm

base: ubuntu@24.04

platforms:
  amd64:

summary: Minimal fake debug-ssh provider for GARM integration tests.

description: |
  Provides the debug-ssh relation with hard-coded tmate server data.
  Used only in integration tests.

provides:
  provide-debug-ssh:
    interface: debug-ssh

parts:
  charm:
    source: .
    plugin: charm

name: Charms Lint and Unit Tests
on:
  pull_request:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  detect-changes:
    runs-on: ubuntu-latest
    outputs:
      charms: ${{ steps.filter.outputs.charms }}
    steps:
      - uses: actions/checkout@v7
      - uses: dorny/paths-filter@v4
        id: filter
        with:
          filters: |
            charms:
              - 'charms/**'
              - 'internal/**'
              - 'tox.ini'
              - '.github/workflows/charms_lint_and_unit.yaml'

  charm-tests:
    needs: detect-changes
    if: ${{ needs.detect-changes.outputs.charms == 'true' }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        charm:
          - charms/planner-operator
          - charms/webhook-gateway-operator
          - charms/garm
          - charms/garm-configurator
    steps:
      - uses: actions/checkout@v7

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: "3.12"

      - name: Set up uv
        uses: astral-sh/setup-uv@v10.0.1

      - name: Install tox
        run: uv tool install tox --with tox-uv

      - name: Run lint, complexity, static, unit, coverage
        working-directory: ${{ matrix.charm }}
        run: tox -c tox.toml

  shared-test-support:
    name: Shared test support unit tests
    needs: detect-changes
    if: ${{ needs.detect-changes.outputs.charms == 'true' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: "3.12"

      - name: Set up uv
        uses: astral-sh/setup-uv@v10.0.1

      - name: Install tox
        run: uv tool install tox --with tox-uv

      - name: Run unit tests for charms/tests/
        run: tox -e charms-tests-unit
